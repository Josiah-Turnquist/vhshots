const styles = (theme) => ({
  root: {
    backgroundColor: theme.palette.background.default,
    marginTop: '-6px',
    position: 'relative',
  },
  wrapper: {
    display: 'flex',
    flexWrap: 'wrap',
    justifyContent: 'center',
  },
});

export default styles;
