const styles = (theme) => ({
    root: {
      display: 'flex',
      justifyContent: 'center',
      color: 'white',
      textAlign: 'center',
    },
    wrapper: {

    },
    carousel: {
      textAlign: 'center',
      color: 'white',
      width: '50vw',
      height: '50vh',
    },
    images: {
      width: '100%',
      height: '100%',
      objectFit: 'cover',
    }
  });

export default styles;
