const styles = (theme) => ({
    root: {
      display: 'flex',
      justifyContent: 'center',
      color: 'white',
      textAlign: 'center',
    },
    wrapper: {
      width: '80vw',
      marginTop: '10px',
      marginBottom: '22px',
      justifyContent: 'center',
    },
    carousel: {
      textAlign: 'center',
      color: 'white',

      objectFit: 'cover',
    },
    images: {
      width: '100%',
      height: '100%',
      objectFit: 'cover',
    },
  });

export default styles;
